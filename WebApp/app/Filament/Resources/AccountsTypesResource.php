<?php

namespace App\Filament\Resources;

use App\Filament\Resources\AccountsTypesResource\Pages;
use App\Filament\Resources\AccountsTypesResource\RelationManagers;
use App\Models\AccountsTypes;
use Filament\Forms;
use Filament\Forms\Form;
use Filament\Resources\Resource;
use Filament\Tables;
use Filament\Tables\Table;
use Illuminate\Database\Eloquent\Builder;
use Illuminate\Database\Eloquent\SoftDeletingScope;

class AccountsTypesResource extends Resource
{
    protected static ?string $model = AccountsTypes::class;

    protected static ?string $navigationIcon = 'heroicon-o-rectangle-stack';

    public static function form(Form $form): Form
    {
        return $form
            ->schema([
                Forms\Components\Section::make()
                    ->schema([
                        Forms\Components\TextInput::make('description')
                            ->required()
                            ->maxLength(255),
                    ])
            ]);
    }

    public static function table(Table $table): Table
    {
        return $table
            ->columns([
                Tables\Columns\TextColumn::make('description')
                    ->searchable(),
            ])
            ->filters([
                //
            ])
            ->actions([
                Tables\Actions\ViewAction::make(),
                Tables\Actions\EditAction::make(),
            ])
            ->bulkActions([
                Tables\Actions\BulkActionGroup::make([
                    Tables\Actions\DeleteBulkAction::make(),
                ]),
            ]);
    }

    public static function getRelations(): array
    {
        return [
            //
        ];
    }

    public static function getPages(): array
    {
        return [
            'index' => Pages\ListAccountsTypes::route('/'),
            'create' => Pages\CreateAccountsTypes::route('/create'),
            'view' => Pages\ViewAccountsTypes::route('/{record}'),
            'edit' => Pages\EditAccountsTypes::route('/{record}/edit'),
        ];
    }
}
